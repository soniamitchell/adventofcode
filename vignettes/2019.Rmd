---
title: '2019'
author: "Sonia Mitchell"
date: "28/12/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

# To set up a virtual environment with reticulate:

# 1. `Sys.setenv(RETICULATE_PYTHON = "/usr/bin/python3")`
# 2. `library(reticulate)`
# 3. `conda_create("aoc2019", python_version = "3.8.2")`
# 4. `py_install("numpy", envname = "aoc2019")`, since reticulate always 
# prefers environments that contain NumPy
# 5. Restart R
# 6. `reticulate::use_condaenv("aoc2019")`
# 7. Check with `py_config()` or `py_discover_config()`

# reticulate::py_install("pandas", envname = "aoc2019")
```

## Day 1: [The Tyranny of the Rocket Equation](https://adventofcode.com/2019/day/1)

### Part 1

```{python 1-1}
False and True
```

ajldh

```{python}
import pandas as pd
import numpy as np

grid = np.random.rand(5,2)
df = pd.DataFrame(grid)

def multiply(x):
    return x[0]*x[1]

df['multiply'] = df.apply(multiply, axis = 1)
print(df)
```
